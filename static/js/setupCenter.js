import { openView, registerView } from './workspaceWindowManager.js';
import { errorState, loadingState, moduleHeader, statusBadge } from './ui-components.js';

const esc = value => String(value ?? '').replace(/[&<>"']/g, c => ({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const api = async (path, options={}) => { const response=await fetch(path,{credentials:'same-origin',headers:options.body?{'Content-Type':'application/json'}:undefined,...options}); const data=await response.json().catch(()=>({})); if(!response.ok) throw Error(data.detail||`Request failed (${response.status})`); return data; };

async function load(el) {
  const body=el.querySelector('.hades-window-body'); body.innerHTML=loadingState('Loading Setup Center…');
  try {
    const [projection, integrations, profileData, permissions]=await Promise.all([api('/api/setup-center/state'),api('/api/setup-center/integrations'),api('/api/setup-center/profiles'),api('/api/setup-center/permissions')]);
    const healthModules=['core.models','core.memory','investigation.osint','technology.network','technology.homelab','communications.telegram','communications.email','communications.calendar','communications.contacts','home.smart-home','business.crm','interaction.voice','advanced.automations'];
    const categories=Object.entries(projection.categories||{}).map(([category,modules])=>`<section class="setup-center-category"><h3>${esc(category)}</h3><div class="hades-record-list">${modules.map(module=>`<article class="hades-record-card setup-center-module"><div><strong>${esc(module.title)}</strong><p>${esc(module.description)}</p><small>${esc(module.status_reason)} · dependencies: ${esc((module.dependencies||[]).join(', ')||'none')}</small></div><div class="setup-module-actions">${statusBadge(module.status,module.status==='CONFIGURED'?'success':module.status==='NEEDS_ATTENTION'||module.status==='DEGRADED'?'warning':'info')}${healthModules.includes(module.id)?`<button type="button" class="setup-health-button" data-health-id="${esc(module.id)}">Validate</button>`:''}<button type="button" class="setup-skip-button" data-setup-id="${esc(module.id)}" data-setup-status="${module.status==='SKIPPED'?'NOT_CONFIGURED':'SKIPPED'}">${module.status==='SKIPPED'?'Resume':'Skip'}</button></div></article>`).join('')}</div></section>`).join('');
    const integrationCards=(integrations.integrations||[]).map(item=>`<article class="hades-record-card"><div><strong>${esc(item.title)}</strong><p>${esc(item.capabilities.join(', ')||'No capabilities recorded')}</p></div><div>${statusBadge(item.connection,item.connection==='CONNECTED'?'success':item.connection==='DEGRADED'?'warning':'info')}</div></article>`).join('');
    const profiles=(profileData.profiles||[]).map(profile=>`<option value="${esc(profile.id)}" ${projection.selected_profile===profile.id?'selected':''}>${esc(profile.id.replaceAll('_',' / '))}</option>`).join('');
    const permissionRows=(permissions.capabilities||[]).map(capability=>`<article class="work-card"><strong>${esc(capability.capability_id)}</strong><small>${esc(capability.description||'')} · ${esc((capability.actions||[]).length)} action(s)</small></article>`).join('');
    body.innerHTML=`${moduleHeader({icon:'settings',title:'Setup Center',description:'See what is ready, what needs connection, and what Hades can use today.',primary:'Refresh',primaryId:'setup-center-refresh'})}<p class="muted">${projection.secrets_exposed?'Secret exposure detected — contact an administrator.':'Secret values are never displayed here.'}</p><section class="setup-center-category"><h3>Setup profile</h3><label>Preselect modules <select id="setup-profile-select"><option value="">Custom</option>${profiles}</select></label><small class="muted">Profiles change selection only; they never grant permissions.</small></section><section class="setup-center-category"><h3>Integration readiness</h3><p class="muted">Connections are shown honestly: unavailable services stay unavailable, and configured services are not called until you validate them.</p><div class="hades-record-list">${integrationCards}</div></section>${categories}<details class="work-technical-record setup-permissions-details"><summary>Technical permissions and authority</summary><p class="muted">${esc(permissions.policy_source||'Canonical policy and approval services remain authoritative.')} · ${esc((permissions.grants||[]).length)} delegated grant(s)</p><div class="hades-record-list">${permissionRows||'<p class="muted">No capability metadata available.</p>'}</div></details>`;
    body.querySelector('#setup-center-refresh').onclick=()=>load(el);
    body.querySelector('#setup-profile-select').onchange=async event=>{if(!event.target.value)return;event.target.disabled=true;try{await api(`/api/setup-center/profiles/${encodeURIComponent(event.target.value)}`,{method:'POST'});await load(el);}catch(error){body.insertAdjacentHTML('afterbegin',errorState(error.message));event.target.disabled=false;}};
    body.querySelectorAll('.setup-skip-button').forEach(button=>button.onclick=async()=>{button.disabled=true;try{await api(`/api/setup-center/modules/${encodeURIComponent(button.dataset.setupId)}`,{method:'PATCH',body:JSON.stringify({status:button.dataset.setupStatus})});await load(el);}catch(error){button.disabled=false;body.insertAdjacentHTML('afterbegin',errorState(error.message));}});
    body.querySelectorAll('.setup-health-button').forEach(button=>button.onclick=async()=>{button.disabled=true;button.textContent='Checking…';try{const result=await api(`/api/setup-center/modules/${encodeURIComponent(button.dataset.healthId)}/health`,{method:'POST'});button.insertAdjacentHTML('afterend',`<small>${esc(result.detail)}</small>`);}catch(error){button.insertAdjacentHTML('afterend',`<small class="security-error">${esc(error.message)}</small>`);}finally{button.disabled=false;button.textContent='Validate';}});
  } catch(error) { body.innerHTML=errorState(error.message,'setup-center-retry'); body.querySelector('#setup-center-retry')?.addEventListener('click',()=>load(el)); }
}

export function openSetupCenter(){const el=openView('setup-center',null,'Setup Center',loadingState());load(el);return el;}
registerView('setup-center',()=>openSetupCenter());
export default {openSetupCenter};
